<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link
            rel="stylesheet"
            href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
            />
        <title>Approachat登録画面</title>
    </head>
    <body class="px-md-5 pt-5">
        <div class="container px-sm-5">
            <h1 class="text-center my-3">ユーザー登録</h1>

            <ul>
                <% if(typeof errors != 'undefined') { %> <% errors.forEach(error=>{ %>
                <li><%= error.message %></li>
                <% }) %> <% } %>
            </ul>

            <form action="/users/register" method="POST">
                <div class="form-group row">
                    <div class="col-md-3">
                        <label for="name1">名前：</label>
                    </div>
                    <div class="col-md">
                        <input class="form-control" type="text" name="name" placeholder="名前" required />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-md-3">
                        <label for="name1">メールアドレス：</label>
                    </div>
                    <div class="col-md">
                        <input class="form-control"
                               type="email"
                               name="email"
                               placeholder="メールアドレス"
                               required
                               />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-md-3">
                        <label>パスワード：</label>
                    </div>
                    <div class="col-md">
                        <input class="form-control"
                               type="password"
                               name="password"
                               placeholder="パスワード"
                               required
                               />
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-md-3">
                        <label>パスワード確認：</label>
                    </div>
                    <div class="col-md">
                        <input class="form-control"
                               type="password"
                               name="password2"
                               placeholder="パスワード確認"
                               required
                               />
                    </div>
                </div>
                <div>
                    <input type="submit" class="btn btn-primary btn-block my-3" value="登録" />
                </div>
                <div class="text-center">
                    <a href="/users/login">ログイン画面はこちらから</a>
                </div>
            </form>
        </div>
    </body>
</html>
